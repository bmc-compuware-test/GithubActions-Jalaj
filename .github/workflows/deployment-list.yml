on: workflow_dispatch

jobs:
  run-ispw-deployment-list:
    runs-on: [self-hosted]
    name: A job to get deployment list of Code Pipeline
    steps:
      - name: Deployment List
        uses: bmc-compuware-test/codepipeline-deployment-list@main
        id: deploymentlist
      
    
